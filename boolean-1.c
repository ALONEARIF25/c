#include <stdio.h>
#include <stdbool.h>

int main(){
  bool sabaSundor = true;
  
  if(sabaSundor) {
      printf("you are on the right path!");
  } else if(!sabaSundor) {
      printf("oops...");
  }

}
